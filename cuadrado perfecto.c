#include <stdio.h>
#include <math.h>
void main ()
{
	int m;
	float n;
	
	printf("Ingrese un numero: ");
	scanf("%i",&m);
	
	if (n=trunc(sqrt(pow(m,2))))
	{
		printf("el numero ingresaso es cuadrado perfecto");	
	else
	    print("el numero ingresado no es cuadrado perfecto");	
	}
}
